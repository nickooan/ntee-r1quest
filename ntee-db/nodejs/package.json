{
  "name": "@ntee/ntee-db",
  "version": "0.1.0",
  "description": "Node.js binding for nteedb — a pure-Go embedded log-structured KV store with secondary indexes",
  "type": "module",
  "main": "src/index.js",
  "types": "src/index.d.ts",
  "files": [
    "src",
    "prebuilds"
  ],
  "scripts": {
    "build:native": "bash ../capi/build.sh",
    "test": "node --test"
  },
  "dependencies": {
    "koffi": "^2.9.0"
  }
}
